# v1–v9 分析报告

> 目标：复现 CFSR（Class Feature Space Reconstruction，自动调制开放集识别）。
> 判定口径（与用户确认）：训练用全 SNR；**成功标准 = SNR≥0 子集上 `known_accuracy ≥ 0.90` 且 `auroc ≥ 0.90`**（均取自 `high_snr_metrics`）。
> 基线（修复前）：known_acc 0.7825 / auroc 0.8856（旧骨干 + 训练期增强 + 全局阈值 + 8知/3未知固定划分）。

## 1. 逐版结果

| 版本 | 改动 | known_acc | auroc | TKR | TUR | 备注 |
|---|---|---|---|---|---|---|
| baseline | 原始 | 0.7825 | 0.8856 | 0.907 | 0.9387 | 旧骨干、增强、全局阈值 |
| v1 | 骨干换真 1D ResNet18 + 清死代码 | 0.8169 | 0.8608 | 0.9216 | 0.8707 | 闭集变强、开集略降 |
| v2 | 去掉训练期增强 | 0.8519 | 0.9143 | 0.9209 | 0.5550 | 排序变好、阈值错位 |
| v3 | 放开全 SNR 训练 | 0.8884* | 0.8984 | 1.0000 | 0.0000 | *虚假高：阈值失效不拒识 |
| v4 | 阈值改 (类别,SNR) 分档 | 0.8230 | 0.8984 | 0.9223 | 0.8400 | 阈值恢复 |
| v5 | 5 折轮换（11类） | 0.7215 | 0.7753 | 0.8612 | 0.4932 | 2折均值；AM-DSB 作未知拖垮 |
| v6-v9 | 去 AM-SSB + 早停 + pos_weight/L1sum/温度/噪声修正 | **0.8140** | **0.9115** | 0.9209 | 0.9513 | 见下 |

- v3 的 0.8884 是假象：全局阈值被低 SNR 大误差抬高到"什么都不拒识"，TUR=0。
- v5 的 2 折均值（0.7215/0.7753）说明：`AM-DSB` 作未知类时几乎不可辨识，是 v6 去 AM-SSB 的直接依据。

## 2. v6–v9 合并效果拆解

| 改动 | 代码位置 | 影响 |
|---|---|---|
| v6 去 AM-SSB 对齐 10b 类别表 | `data.py:MODULATIONS` | 消除一个不可辨识的未知类，TUR 恢复 |
| v6 早停 patience=5 | `main.py` / `train.py` | 遏制全 SNR 过拟合 |
| v7 去 BCE `pos_weight` | `losses.py` | TUR 0.84 → 0.95，阈值放正 |
| v8 L1 mean→sum + 温度 12.8 | `model.py` / `losses.py` | 更贴论文 ‖·‖₁ 求和；温度显式化 |
| v9 AE 噪声 0 | `train.py` | 消除训练/评测误差尺度不一致 |

## 3. 结论：为什么卡在 0.814 而不是 0.90

1. **`known_accuracy` 被 TKR 封顶**。SNR≥0 子集上 TKR=0.9209，意味着最多 0.9209；要到 0.90 要求 SNR≥0 内闭集正确率 ≥ 0.977，极难。
2. **SNR≥0 里还混着 0~6 dB 的难样本**。全 SNR 训练把容量摊到 -20 dB 噪声上，高 SNR 段的判别力反被稀释。
3. **全 SNR 训练 / 校准 ≠ 判定口径**。校准用全 SNR 数据，与"只看 SNR≥0"的判定目标错位。

## 4. 迭代方向（v10 起）

1. **训练/校准收紧到 SNR≥0**：与判定口径对齐，直接优化被评价区域。AUROC 已 0.91，主要补 known_acc。
2. 若仍需全 SNR 曲线，作为附加输出，不作为主训练口径。
3. 提高 SNR≥0 闭集正确率：更充分训练高 SNR 段、必要时增加 epoch 或调低 open_weight 让分类分支更强。
